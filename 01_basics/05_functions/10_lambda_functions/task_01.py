# Input:
# Declare an anonymous (lambda) function with one parameter
# that squares a number.
# Assign this function to the variable get_sq.
#
# You do not need to call the function, only declare it.

get_sq = lambda x: x**2