# Input:
# A string is given.
#
# Output:
# Declare a function named is_large that accepts one parameter.
# The function must return False if the length of the string is less than 3 characters.
# Otherwise, it must return True.
#
# The function must not be called.
#
# Input:
# I love Python!
#
# Output:
# True

def is_large(text):
    return len(text) >= 3